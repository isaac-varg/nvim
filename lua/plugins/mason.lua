-- Mason allows us to easily install language servers rather than installing them on our local environment and manually configuring them to interact with neovim

return {
    "williamboman/mason.nvim",
        opts = {}
}
