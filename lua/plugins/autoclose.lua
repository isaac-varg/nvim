-- lightweight autoclose/autopair plugin
-- has no tree sitter integrations
-- might switch out for -- lightweight autoclose/autopair plugin
-- has no tree sitter integrations https://github.com/windwp/nvim-autopairs
-- but this one was simplet to set up
return {
        "m4xshen/autoclose.nvim",
        opts = { }
}
